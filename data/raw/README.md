# Raw Data

Place the source CSV files here when running the project from inside this folder:

- `train_details.csv`
- `station_full_names.csv`
- `combined_schedule.csv`
- `combined_delay.csv`

The backend also checks the parent `projects` folder by default, so the CSV files can stay where they are now.
